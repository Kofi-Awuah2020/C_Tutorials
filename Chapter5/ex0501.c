#include <stdio.h>

int main(void)
{
    //Using place holders or conversion characters
    printf("The value %d is an integer.\n", 986);
    printf("The value %2.1f is a float.\n", 98.6); 

/*  The '2.1' b/n '%' & 'f', formats output with 2 digits to the left 
    and one digit to the right of the decimal point  */

    return(0);
}